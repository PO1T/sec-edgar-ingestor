# Next Steps

1. Run live database integration validation once Python 3.10, `psycopg`, and PostgreSQL are available in the target environment.
2. Add richer ingestion run metrics and data quality warning summaries.
3. Add SQL example queries for top holders, quarter-over-quarter changes, and option aggregations.
4. Design the first enrichment layer for ticker aliases and canonical instrument mapping.
5. Implement the next filing family, likely `144` or `N-PORT`, on the shared acquisition/storage foundation.
